#include <stdio.h>
int main(int argc, char const *argv[])
{
	int n;
	printf("Enter the number of days you wnat to record: ");
	scanf("%d",&n);
	int num[n];
	average()
	return 0;
}
int average(array[int n]){
	int total;
	float avg;
	for(i=0;i<n;i++){
		total+=array[i];
	}
	avg = total/n ;
	return avg;
}